# RPostgreSQL is operating on R 3.0.2 !!
# par(family="AppleGothic") for using hangul on mac
# Set for
# sessionInfo()
# Sys.getlocale() and Sys.setlocale('LC_ALL','en_US.UTF-8')
#
# install.packages('cairoDevice',INSTALL_opts="--no-test-load")
